// ============ CONFIG ============
// Yahan sirf BACKEND ka link daalna hai (Render pe deploy karne ke baad milta hai).
window.APP_CONFIG = {
  // backend ka link — trail slash ke BINA.
  // Example: "https://tg-stream-backend-abc123.onrender.com"
  BACKEND: "",

  // videos ki list wali file (isko mat chhedna)
  DATA_URL: "data/videos.json",

  // 📢 [Optional] Website ke upar ek sukhadita banner dikhana ho to yahan likho.
  // Khali chhodo to banner nahi aayega. Example:
  // ANNOUNCE: "🎉 Naya batch aa gaya — GS Foundation 2026",
  ANNOUNCE: "",
};